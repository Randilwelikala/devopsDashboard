@page @model IndexModel @{ ViewData["Title"] = "SigNEX DevOps Dashboard"; Layout = null; // Use full HTML structure }
SigNEX Client Monitoring Dashboard
Select a client below to view detailed performance metrics.

Data simulated as of: @Model.FullData.GeneratedTime.ToLocalTime().ToString("g")

Active Clients (@Model.ClientInstances.Count)
@foreach (var instance in Model.ClientInstances) { // Use a button for accessibility and data attributes to hold the client ID 
@instance.ClientName

@instance.Status.ToString()
@instance.Region
 }
Client Detail View
@foreach (var instance in Model.ClientInstances) { // Render all detail cards but hide them by default (except the first one) }